server:
  port: 8080

spring:
  application:
    name: toolize
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

toolize:
  data-dir: ${TOOLIZE_DATA_DIR:/data}

logging:
  level:
    root: INFO
    com.toolize: INFO
